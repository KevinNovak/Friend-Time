export enum ServerConfigName {
    mode = 'mode',
    format = 'format',
    notify = 'notify',
}

export enum ModeOption {
    react = 'react',
    list = 'list',
}

export enum FormatOption {
    twelve = '12',
    twentyFour = '24',
}

export enum NotifyOption {
    on = 'on',
    off = 'off',
}
